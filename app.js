
const $=(s)=>document.querySelector(s);const clsForStatus=s=>{if(s.includes('상승')||s.includes('강함')||s.includes('개선'))return'state-good';if(s.includes('약화')||s.includes('부담')||s.includes('위험'))return'state-bad';if(s.includes('초기')||s.includes('주의')||s.includes('냉각'))return'state-warn';return'state-mid'};const chipClass=s=>s.includes('상승')?'upcycle':s.includes('초기')?'early':s.includes('약화')?'weak':'neutral';
async function load(){const r=await fetch(`data/dashboard.json?t=${Date.now()}`);const d=await r.json();$('#updatedAt').textContent=`업데이트 ${d.updated_at_kst||'-'}`;$('#marketStrip').innerHTML=d.market.map(m=>`<div class="market-item"><div class="market-name">${m.name}</div><div class="market-value">${m.value}</div><div class="market-change ${m.change_pct>=0?'positive':'negative'}">${m.change_pct>=0?'+':''}${m.change_pct.toFixed(2)}%</div></div>`).join('');$('#regimeName').textContent=d.regime.name;$('#regimeReasons').innerHTML=d.regime.reasons.map(x=>`• ${x}`).join('<br>');$('#macroCards').innerHTML=d.macro.map(m=>`<div class="macro-card panel"><div class="macro-top"><div class="macro-title">${m.name}</div><div class="macro-state ${clsForStatus(m.state)}">${m.state}</div></div><div class="macro-sub">${m.summary}</div></div>`).join('');$('#sectorTable').innerHTML=d.sectors.map(s=>`<tr><td class="sector-name">${s.name}</td><td><span class="chip ${chipClass(s.status)}">${s.status}</span></td><td class="score-cell">${s.score}</td><td><b>${s.etfs.join(' / ')}</b></td><td class="action">${s.action}</td></tr>`).join('');$('#sectorCards').innerHTML=d.sectors.map(s=>`<div class="sector-card"><div class="sector-card-top"><div class="sector-name">${s.name}</div><span class="chip ${chipClass(s.status)}">${s.status}</span></div><div style="margin-top:8px"><b>${s.score}점</b> · ${s.etfs.join(' / ')}</div><small>${s.action}</small></div>`).join('');const top=d.watchlist||d.sectors.slice().sort((a,b)=>b.score-a.score).slice(0,4);$('#watchList').innerHTML=top.map((w,i)=>`<div class="watch-card ${i===0?'selected':''}" data-symbol="${w.symbol||w.etfs?.[0]||''}"><div class="watch-head"><div><div class="symbol">${w.symbol||w.etfs?.[0]||'-'}</div><div class="watch-meta">${w.name||''}</div></div><span class="chip ${chipClass(w.status)}">${w.status}</span></div><div class="watch-action">${w.action}</div></div>`).join('');const details={};d.sectors.forEach(s=>s.etfs.forEach(e=>details[e]=s));document.querySelectorAll('.watch-card').forEach(card=>{card.onclick=()=>{document.querySelectorAll('.watch-card').forEach(x=>x.classList.remove('selected'));card.classList.add('selected');renderDetail(details[card.dataset.symbol]||d.sectors[0])}});renderDetail(details[top[0]?.symbol]||d.sectors[0]);$('#events').innerHTML=(d.events||[]).map(e=>`<div class="event"><b>${e.name}</b><span>${e.when}</span></div>`).join('')}
function renderDetail(s){$('#detailTitle').textContent=`${s.etfs[0]} · ${s.name}`;const factors=[['가격 모멘텀',s.factors.momentum],['상대강도',s.factors.relative_strength],['거래량/수급',s.factors.volume],['Macro 환경',s.factors.macro]];$('#factorBars').innerHTML=factors.map(([n,v])=>`<div class="factor"><div class="factor-name">${n}</div><div class="bar"><div class="fill" style="width:${v}%"></div></div><div class="factor-score">${v}</div></div>`).join('');$('#detailScore').textContent=`${s.score}/100`;$('#detailConclusion').textContent=`${s.status} · ${s.action}`;$('#detailNote').textContent=s.reason||'가격·상대강도·거래량·거시환경을 가중 합산한 v1 점수입니다.'}load().catch(err=>{console.error(err);document.body.insertAdjacentHTML('beforeend',`<div style="position:fixed;bottom:10px;left:10px;background:#571b1b;color:white;padding:10px;border-radius:8px">데이터 로딩 오류: ${err.message}</div>`)});
